<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Category</title>
</head>
<body>
    <%- include('./partials/nav') %>
  <h1>Category</h1>
  <% menuItems.forEach(item => { %> 
    <a href="<%= item %>">
      <h2><li><%= `${item.name} $ ${item.price}`%> </li></h2>

  <% }); %>

</body>
</html>
